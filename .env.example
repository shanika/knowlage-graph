# Absolute or relative path to the markdown vault to render.
# Resolved relative to the apps/api working directory at runtime.
VAULT_PATH=../../vault

# Port the Express API listens on. The web app's Vite dev server proxies /api here.
API_PORT=4000
